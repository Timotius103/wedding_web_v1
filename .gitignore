# Firebase/service-account credentials must never be committed.
service-account*.json
serviceAccount*.json
*firebase-adminsdk*.json
firebase-admin-setup/

# Local Node dependencies, if a one-off tool is used outside this static app.
node_modules/
